<h1>Reservation list for __</h1>
<section>
  <%= month_calendar(attribute: :start_date) do |date| %>
    <%= date %>
    <% @reservations.select{|r| r.start_date == date}.each do |res| %>
      <%= res.guest_email %>
    <% end %>
  <% end %>
</section>
