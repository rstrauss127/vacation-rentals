<%= form_for(new_listing_reservation_path) do |f| %>
  <%= f.label "start date" %>
  <%= f.text_field :start_date%>

  <%= f.label "end date" %>
  <%= f.text_field :end_date %>

  <%= f.hidden_field :guest_id, :value => current_user.id %>

  <%= f.submit "New Reservation" %>
<% end %>
