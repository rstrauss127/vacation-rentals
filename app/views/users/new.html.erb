<section class="todoapp">
  <header class="header">
    <h1>Sign Up</h1>
    <%= form_for(@user) do |f| %>
      <%= f.text_field :email, class: 'new-todo', placeholder: "Register with your email" %>

      <%= f.label "Owner" %>
      <%= f.check_box :owner, class: 'new-todo' %>

      <%= f.text_field :password, class: 'new-todo', placeholder: "Enter password" %>
      <%= f.submit %>
    <% end %>
  </header>
</section>
