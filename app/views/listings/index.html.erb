
  <header class="header">
		<h1>Listings</h1>
    <%= link_to "New Listing", new_listing_path, class: "new-todo" %>
	</header>

<button id="getListings">Get Listings</button>
    <ul class="todo-list">
      <% @listings.each do |list| %>
        <div id="listing-<%= list.id %>">

    <% end %>
    </ul>



<%= render "partials/footer" %>
